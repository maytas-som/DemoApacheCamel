# ===================================================================
# Spring boot properties
# ===================================================================

spring:
  application:
    name: camelDemo
  jackson:
    property-naming-strategy: SNAKE_CASE

server:
  context-path: /camel-demo
  port: 8080

camel:
  component:
    servlet:
      mapping:
        enabled: true
        context-path: /api/*